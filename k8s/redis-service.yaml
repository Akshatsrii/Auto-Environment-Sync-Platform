apiVersion: v1
kind: Service
metadata:
  name: devsync-redis-service
  labels:
    app: devsync-redis
spec:
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: devsync-redis
  type: ClusterIP
