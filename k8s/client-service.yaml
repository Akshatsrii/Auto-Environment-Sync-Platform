apiVersion: v1
kind: Service
metadata:
  name: devsync-client-service
  labels:
    app: devsync-client
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: devsync-client
  type: ClusterIP
